import classNames from 'classnames/bind';
import styles from './SlidePromotion.module.scss';

const cx = classNames.bind(styles);

export default function SlidePromotion() {
    return <div>SlidePromotion</div>;
}
